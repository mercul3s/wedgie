An elasticsearch Ruby client. 

Currently: support for query string, mappings, and counts.
Eventually: Support for querying against different analyzers, changing Elasticsearch server settings, query dsl

